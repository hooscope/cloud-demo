server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_member?useSSL=false
    username: root
    password: 12345678
    driver-class-name: com.mysql.jdbc.Driver
  application:
    name: memberservice # ????
#  zipkin:
#    base-url: http://localhost:9411
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos????
      discovery:
        cluster-name: HZ
#opentracing:
#  jaeger:
#    enabled: true
#    udp-sender:
#      host: localhost
#      port: 6831
mybatis:
  type-aliases-package: cn.itcast.member.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS